/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.{js,jsx}"], // Scans HTML and your JS/JSX files for classes
  theme: {
    extend: {}, // Extend themes here later (e.g., custom colors)
  },
  plugins: [], // Add plugins like typography if needed
};